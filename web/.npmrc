# Ensure legacy peer deps resolution for compatibility
legacy-peer-deps=false

# Strict SSL
strict-ssl=true

# Auto-install peer dependencies
auto-install-peers=true

# Save exact versions
save-exact=false

# Engine strict (Vercel compatibility)
engine-strict=false

